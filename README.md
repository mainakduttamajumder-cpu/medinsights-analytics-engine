# medinsights-analytics-engine
An end-to-end healthcare data analytics pipeline for structured medical datasets. This repository performs automated data cleaning, duplicate removal, null handling, datetime sorting, dimensionality reduction (PCA), anomaly detection (Isolation Forest), clustering (KMeans), and generates a fully structured PDF analytics report with visualizations.
